{
  "dependencies": {
    "tailwindcss": "^3.3.2"
  },
  "scripts": {
    "build": "tailwind --minify --input=./templates/site.css --output=./static/css/site.css && zola build",
    "dev": "tailwind --watch --input=./templates/site.css --output=./static/css/site.css & zola serve",
    "watch": "tailwind --watch --input=./templates/site.css --output=./static/css/site.css",
    "serve": "zola serve"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.9"
  }
}
